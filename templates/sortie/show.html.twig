{% extends 'base.html.twig' %}

{% block title %}Sortie{% endblock %}

{% block body %}
    <h1>Sortie</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ sortie.nom }}</td>
            </tr>
            <tr>
                <th>Date_heure_debut</th>
                <td>{{ sortie.dateHeureDebut ? sortie.dateHeureDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ sortie.duree }}<p>minutes</p></td>
            </tr>
            <tr>
                <th>Date_limite_inscription</th>
                <td>{{ sortie.dateLimiteInscription ? sortie.dateLimiteInscription|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nb_inscription_max</th>
                <td>{{ sortie.nbInscriptionMax }}<p>personnes</p></td>
            </tr>
            <tr>
                <th>Infos_sortie</th>
                <td>{{ sortie.infosSortie }}</td>
            </tr>
            <tr>
                <th>Campus</th>
                <td>{{ sortie.campus.nom }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ sortie.lieu.nom }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ sortie.lieu.rue }}</td>
            </tr>
            <tr>
                <th>Code postal</th>
                <td>{{ sortie.ville.cp }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ sortie.lieu.latitude }}</td>
            </tr>
            <tr>
                <th>longitude</th>
                <td>{{ sortie.lieu.longitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sortie_index') }}">retour à la liste</a>

    <a href="{{ path('sortie_edit', {'id': sortie.id}) }}">editer</a>

    {{ include('sortie/_delete_form.html.twig') }}
{% endblock %}
