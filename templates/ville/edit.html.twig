{% extends 'base.html.twig' %}

{% block title %}Editer Ville{% endblock %}

{% block body %}
    <h1>Editer Ville</h1>

    {{ include('ville/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary" href="{{ path('ville_index') }}">retour à la liste</a>

    {{ include('ville/_delete_form.html.twig') }}
{% endblock %}
