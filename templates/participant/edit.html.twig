{% extends 'base.html.twig' %}

{% block title %}Editer Participant{% endblock %}

{% block body %}
    <h1>Editer Participant</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary" href="{{ path('participant_index') }}">retour à la list</a>

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
