{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}

    <h1>Campus</h1>

    <table class="table table-hover table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campus.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('campus_index') }}">retour à la liste</a>

    <a class="btn btn-primary" href="{{ path('campus_edit', {'id': campus.id}) }}">editer</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
